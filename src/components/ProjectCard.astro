---
import HoverEffect from './HoverEffect';

interface Props {
  title: string;
  imageUrl: string;
  description: string;
  technologies: string[];
  documentacion?: string;
  githubUrl: string;
  projectUrl?: string;
}

const { title, imageUrl, description, technologies, documentacion,githubUrl, projectUrl } = Astro.props;
---

<HoverEffect
  client:load
  title={title}
  imageUrl={imageUrl}
  description={description}
  technologies={technologies}
  documentacionUrl={documentacion}
  githubUrl={githubUrl}
  projectUrl={projectUrl}
/>